# vehicle-gateway-c_car_platform

Vehicle gateway service container (placeholder).

## Local development

### Environment
Copy and edit:
- `.env.example` -> `.env`

### Commands (planned)
If/when a Node.js implementation is added:

```bash
npm install
npm run dev
npm run lint
npm run format
npm test
```
