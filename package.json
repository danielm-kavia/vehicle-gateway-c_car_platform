{
  "name": "@connected-car/vehicle-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Vehicle gateway service (Phase 3 MVP): optional publisher stub for telematics to Kafka or ingestion HTTP.",
  "main": "src/index.js",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "node src/index.js",
    "start": "node src/index.js",
    "lint": "node -e \"console.log('lint placeholder (Phase 3)')\"",
    "test": "jest --passWithNoTests",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "@connected-car/shared": "file:../shared-c_car_platform",
    "express": "4.21.2",
    "kafkajs": "^2.2.4"
  },
  "devDependencies": {
    "jest": "^30.0.0"
  }
}
